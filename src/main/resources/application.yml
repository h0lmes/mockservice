server:
  port: 8081

logging:
  level:
    ROOT: ${LOGGING_LEVEL:INFO}

spring:
  application:
    name: MockService

  thymeleaf:
    cache: false
    enabled: true
    prefix: classpath:/webapp/
    suffix: .html

application:
  config:
    resource: classpath:defaultConfig.yml
    file: .\config.yml
  soap-error-data-file: soapFault.xml
  cache:
    rest-resource: 256
    soap-resource: 256
