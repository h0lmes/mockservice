{
  "name": "webapp",
  "version": "1.0.0",
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "start": "nuxt start",
    "cleandist": "rimraf dist/*",
    "clean": "rimraf ../src/main/resources/webapp/*",
    "copy": "cpx dist/** ../src/main/resources/webapp/",
    "all": "npm-run-all build clean copy"
  },
  "dependencies": {
    "nuxt": "^2.15.5"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "npm-run-all": "^4.1.3",
    "rimraf": "^2.6.2"
  }
}
